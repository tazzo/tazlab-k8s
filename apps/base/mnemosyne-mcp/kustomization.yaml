apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

  - service.yaml
  - rbac.yaml

patches:
  - path: ../../../infrastructure/common/wait-for-db-patch.yaml
    target:
      kind: Deployment
      name: mnemosyne-mcp
