apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: teaching
  namespace: flux-system
spec:
  image: tazzo/tazlab-teaching
  interval: 1m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: teaching
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: teaching
  policy:
    numerical:
      order: asc
  filterTags:
    pattern: '^teaching-(?P<value>[0-9]+)-.*$'
    extract: '$value'
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: teaching
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  git:
    checkout:
      ref:
        branch: master
    commit:
      author:
        email: fluxcdbot@tazlab.net
        name: fluxcdbot
      messageTemplate: 'chore: self-update teaching image [skip ci]'
    push:
      branch: master
  update:
    path: ./apps/base/teaching
    strategy: Setters
