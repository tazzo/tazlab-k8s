apiVersion: v1
kind: Namespace
metadata:
  name: hugo-blog
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: wildcard-tls-external
  namespace: hugo-blog
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical-tazlab
  target:
    name: blog-${domain}-tls
    template:
      type: kubernetes.io/tls
  data:
    - secretKey: tls.crt
      remoteRef:
        key: WILDCARD_CRT
    - secretKey: tls.key
      remoteRef:
        key: WILDCARD_KEY
