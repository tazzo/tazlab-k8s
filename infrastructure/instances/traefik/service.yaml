apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  namespace: traefik
  spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: traefik
  ports:
    - port: 8003
      targetPort: 8080
