apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: ai-agents
  annotations:
    metallb.universe.tf/loadBalancerIPs: "192.168.1.242"
spec:
  type: LoadBalancer
  selector:
    app: openclaw
  ports:
    - protocol: TCP
      port: 18789
      targetPort: 18789
      name: gateway
