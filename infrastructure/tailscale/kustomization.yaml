apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
# externalsecret.yaml removed: OAuth secret pre-created by Terraform engine layer to break
# circular dependency (Tailscale Operator DNS → Vault → ESO → Operator)
