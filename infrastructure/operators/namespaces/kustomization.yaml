apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# NOTE: All namespaces sono stati spostati inline nelle cartelle operator (Opzione A.1)
# - cloudflare-ddns → cloudflare-ddns/
# - tazlab-db → tazlab-db/
# - hugo-blog → hugo-blog/
# - dex → dex/
# - auth → auth/
# - monitoring → monitoring/
