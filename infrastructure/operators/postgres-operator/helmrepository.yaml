apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: crunchydata-repo
  namespace: flux-system
spec:
  interval: 1h
  url: oci://registry.developers.crunchydata.com/crunchydata
  type: oci
