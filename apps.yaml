apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-configs
  interval: 1h
  path: ./apps/cluster
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 5m
