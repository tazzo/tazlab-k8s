apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-bridge
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-operators-core
    - name: infrastructure-operators-namespaces
  interval: 1h
  path: ./infrastructure/cluster-bridge
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars
        optional: false
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 5m
