apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-operators-tailscale
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-tailscale
  interval: 1h
  path: ./infrastructure/operators/tailscale
  prune: true
  wait: true
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 5m
