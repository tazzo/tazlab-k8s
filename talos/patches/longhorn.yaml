machine:
  kernel:
    modules:
      - name: iscsi_tcp
      - name: nbd
      - name: iscsi_generic
      - name: configfs
  
  kubelet:
    extraMounts:
      - destination: /var/lib/longhorn
        type: bind
        source: /var/mnt/longhorn
        options:
          - bind
          - rshared
          - rw
  
  disks:
    - device: /dev/vda
      partitions:
        - mountpoint: /var/mnt/longhorn